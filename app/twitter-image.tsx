/*
 * Twitter share card. Identical to the Open Graph image, re-exported rather than
 * duplicated so the two never drift. Next's file convention picks this up for
 * `twitter.images`.
 */
export { default, alt, size, contentType } from "./opengraph-image";
